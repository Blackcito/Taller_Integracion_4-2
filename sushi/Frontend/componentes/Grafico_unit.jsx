import React from "react";
import { View, StyleSheet, Text } from "react-native";
import Grafico from "./Graficos_init";

export default function Graficos() {
  return (
      <Text>soy un grafico solito</Text>
  );
}

const styles = StyleSheet.create({
  chartContainer: {
    flex: 1
  }
});
